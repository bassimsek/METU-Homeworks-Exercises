package e209880;
/** Enumeration class that represents zombies' states with three instances.
 * These instances are WANDERING and FOLLOWING. */
public enum ZombieState {
    WANDERING,
    FOLLOWING;
}